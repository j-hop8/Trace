/// <reference types="vite/client" />

// Gives TypeScript `import.meta.env` (DEV, PROD, MODE …) and the typings for Vite's asset
// imports, including the `*.json` import the basemap style relies on.
