; A mismatched engines.node is a warning by default -- easy to scroll past, and exactly the
; failure mode this project's Node upgrade (T-008) exists to prevent silently recurring.
engine-strict=true
